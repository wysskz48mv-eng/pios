// Redirects to /api/live/veritasedge — legacy route kept for backward compatibility
import { createClient } from '@/lib/supabase/server'
// VeritasIQ Technologies Ltd
export { GET } from '../veritasedge/route'
